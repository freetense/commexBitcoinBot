import { Vue } from "vue-property-decorator";
export default class extends Vue {
    readonly value: any;
    readonly nudgeLeft: string | number;
    readonly nudgeTop: string | number;
    readonly more: boolean;
    readonly dark: boolean;
    menu: boolean;
    colors: string[];
    picker: boolean;
    setColor(color: any): void;
}
