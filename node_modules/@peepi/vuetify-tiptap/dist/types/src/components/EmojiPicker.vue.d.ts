import { Vue } from "vue-property-decorator";
export default class extends Vue {
    value: boolean;
    readonly activator: any;
    readonly dark: boolean;
    absoluteX: number;
    absoluteY: number;
    apply(emoji: any): void;
    onValueChange(): Promise<void>;
}
